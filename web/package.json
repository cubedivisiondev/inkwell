{
  "name": "inkwell-web",
  "private": true,
  "description": "INKWELL browser front end. Static served-is-source, no build step, no dependencies, no network calls.",
  "scripts": {
    "dev": "npx http-server . -p 5199 -c-1 -o"
  }
}
